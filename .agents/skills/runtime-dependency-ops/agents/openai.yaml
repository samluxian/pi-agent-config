interface:
  display_name: "Runtime Dependency Ops"
  short_description: "Inspect runtime dependencies and deployed source/runtime contracts"
  default_prompt: "Use $runtime-dependency-ops to perform read-only runtime checks across Kubernetes workloads, deployed application startup/source-runtime contracts, identity, secret references, databases, queues, caches, storage, and workers. Inspect source only after application-level evidence maps the running image to its deployed revision."
