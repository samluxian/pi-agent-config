# Pin this to an upstream SearXNG image release after initial verification.
SEARXNG_VERSION=latest

# Loopback only. Do not expose this port publicly without an authenticated
# reverse proxy, TLS, rate limits, and an access policy.
SEARXNG_HOST=127.0.0.1
SEARXNG_PORT=8080
